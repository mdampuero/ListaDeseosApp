import { FilterIsCompletePipe } from './filter-is-complete.pipe';

describe('FilterIsCompletePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterIsCompletePipe();
    expect(pipe).toBeTruthy();
  });
});
